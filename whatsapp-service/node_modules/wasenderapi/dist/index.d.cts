interface BaseMessage {
    /** Recipient phone number in E.164 format, Group JID, or Community Channel JID. */
    to: string;
    /** Optional caption or message text. Rules vary by message type. */
    text?: string;
}
interface TextOnlyMessage extends BaseMessage {
    messageType: 'text';
    /** The text content of the message. Mandatory for plain text messages. */
    text: string;
}
interface ImageUrlMessage extends BaseMessage {
    messageType: 'image';
    /** URL of the image to send. Supported: JPEG, PNG. Max size: 5MB. */
    imageUrl: string;
}
interface VideoUrlMessage extends BaseMessage {
    messageType: 'video';
    /** URL of the video to send. Supported: MP4, 3GPP. Max size: 16MB. */
    videoUrl: string;
}
interface DocumentUrlMessage extends BaseMessage {
    messageType: 'document';
    /** URL of the document to send. Supports PDF, DOCX, XLSX, etc. Max size: 100MB. */
    documentUrl: string;
}
interface AudioUrlMessage extends BaseMessage {
    messageType: 'audio';
    /** URL of the audio file to send (sent as voice note). Supported: AAC, MP3, OGG, AMR. Max size: 16MB. */
    audioUrl: string;
}
interface StickerUrlMessage extends BaseMessage {
    messageType: 'sticker';
    /** URL of the sticker (.webp) to send. Max size: 100KB. */
    stickerUrl: string;
    text?: never;
}
interface ContactCardPayload {
    /** Name of the contact. */
    name: string;
    /** Phone number for the contact. */
    phone: string;
}
interface ContactCardMessage extends BaseMessage {
    messageType: 'contact';
    /** Contact card object. */
    contact: ContactCardPayload;
}
interface LocationPinPayload {
    /** Latitude of the location. */
    latitude: number | string;
    /** Longitude of the location. */
    longitude: number | string;
    /** Optional name for the location. */
    name?: string;
    /** Optional address for the location. */
    address?: string;
}
interface LocationPinMessage extends BaseMessage {
    messageType: 'location';
    /** Location object. */
    location: LocationPinPayload;
}
/** Union of all specific message payload types. Use this for the generic `send` method. */
type WasenderMessagePayload = TextOnlyMessage | ImageUrlMessage | VideoUrlMessage | DocumentUrlMessage | AudioUrlMessage | StickerUrlMessage | ContactCardMessage | LocationPinMessage;
type TextMessage = TextOnlyMessage;
type ImageMessage = ImageUrlMessage;
type VideoMessage = VideoUrlMessage;
type DocumentMessage = DocumentUrlMessage;
type AudioMessage = AudioUrlMessage;
type StickerMessage = StickerUrlMessage;
type ContactMessage = ContactCardMessage;
type LocationMessage = LocationPinMessage;
type ContactCard = ContactCardPayload;
type LocationPin = LocationPinPayload;
/** Standard API success response structure. */
interface WasenderSuccessResponse$1 {
    success: true;
    message: string;
}
/** Rate Limit Information extracted from response headers. */
interface RateLimitInfo {
    /** Max requests per window (X-RateLimit-Limit). */
    limit: number | null;
    /** Remaining requests in the current window (X-RateLimit-Remaining). */
    remaining: number | null;
    /** Unix timestamp (in seconds) when the window resets (from X-RateLimit-Reset). */
    resetTimestamp: number | null;
    /**
     * Optional helper to get the reset timestamp as a Date object.
     * @returns A Date object if resetTimestamp is available, otherwise null.
     */
    getResetTimestampAsDate?: () => Date | null;
}
/** Combined result for a successful API call, including the response body and rate limit info. */
interface WasenderSendResult {
    response: WasenderSuccessResponse$1;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}

interface GroupParticipant {
    /** The id (Jabber ID) of the participant. */
    id: string;
    /** Indicates the admin status of the participant (e.g., "admin", "superadmin"). Undefined if not an admin. */
    admin?: "admin" | "superadmin";
}
interface BasicGroupInfo {
    /** The id (Jabber ID) of the group (e.g., '123456789-987654321@g.us'). */
    id: string;
    /** The name or subject of the group. */
    name: string | null;
    /** URL of the group's profile picture, if available. */
    imgUrl: string | null;
}
interface GroupMetadata$1 extends BasicGroupInfo {
    /** Timestamp of when the group was created. */
    creation: number;
    /** The JID of the group owner/creator. */
    owner: string | undefined;
    /** The description of the group. */
    desc?: string | null;
    /** The owner of the group description. */
    descOwner?: string | null;
    /** The ID of the group description. */
    descId?: string | null;
    /** Whether the group is restricted to admin-only messages. */
    restrict?: boolean;
    /** Whether announcements are enabled for the group. */
    announce?: boolean;
    /** Size of the group. */
    size?: number;
    /** Owner of the group subject. */
    subjectOwner?: string;
    /** Timestamp when subject was last changed. */
    subjectTime?: number;
    /** Array of participants in the group. */
    participants: GroupParticipant[];
    /** The subject of the group. */
    subject: string;
}
interface UpdateGroupSettingsPayload {
    /** New group subject. */
    subject?: string;
    /** New group description. */
    description?: string;
    /** Set to true for admin-only messages, false otherwise. */
    announce?: boolean;
    /** Set to true to restrict editing group info to admins, false otherwise. */
    restrict?: boolean;
}
/** Status of an action (add/remove) for a single participant. */
interface ParticipantActionStatus {
    /** HTTP-like status code for the operation on this participant. */
    status: number;
    /** ID of the participant. */
    id: string;
    /** Message describing the result (e.g., 'added', 'removed', 'not-authorized'). */
    message: string;
}
interface UpdateGroupSettingsResponseData {
    /** The updated subject of the group. */
    subject?: string;
    /** The updated description of the group. */
    description?: string;
}
/** Response for retrieving a list of all groups. */
interface GetAllGroupsResponse extends WasenderSuccessResponse$1 {
    data: BasicGroupInfo[];
}
/** Response for retrieving metadata of a single group. */
interface GetGroupMetadataResponse extends WasenderSuccessResponse$1 {
    data: GroupMetadata$1;
}
/** Response for retrieving participants of a single group. */
interface GetGroupParticipantsResponse extends WasenderSuccessResponse$1 {
    data: GroupParticipant[];
}
/** Response for adding or removing group participants. */
interface ModifyGroupParticipantsResponse extends WasenderSuccessResponse$1 {
    data: ParticipantActionStatus[];
}
/** Response for updating group settings. */
interface UpdateGroupSettingsResponse extends WasenderSuccessResponse$1 {
    data: UpdateGroupSettingsResponseData;
}
interface GetAllGroupsResult {
    response: GetAllGroupsResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface GetGroupMetadataResult {
    response: GetGroupMetadataResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface GetGroupParticipantsResult {
    response: GetGroupParticipantsResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface ModifyGroupParticipantsResult {
    response: ModifyGroupParticipantsResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface UpdateGroupSettingsResult {
    response: UpdateGroupSettingsResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}

/**
 * The HTTP header name used by Wasender to send the webhook signature.
 */
declare const WEBHOOK_SIGNATURE_HEADER = "x-webhook-signature";
/**
 * Verifies the webhook signature from Wasender.
 *
 * @remarks
 * IMPORTANT: The current Wasender documentation example shows a direct string comparison
 * for the signature and secret (`signature === webhookSecret`). This is a very simple
 * verification method. Most webhook systems use HMAC-SHA256 or similar cryptographic hashes
 * for security.
 *
 * **Please VERIFY with Wasender's official documentation or support if this simple string
 * comparison is indeed the correct and only method for signature verification.**
 * If they use HMAC, this function will need to be updated to use Node.js `crypto` module
 * or a similar library (like Web Crypto API for browser/worker environments) to compute
 * and compare HMAC hashes. This function currently implements the simple check as per
 * the provided example.
 *
 * @param requestSignature - The signature string from the `X-Webhook-Signature` header of the incoming request.
 * @param configuredSecret - Your webhook secret configured in the Wasender dashboard.
 * @returns `true` if the signature is valid according to the simple check, `false` otherwise.
 */
declare function verifyWasenderWebhookSignature(requestSignature: string | undefined | null, configuredSecret: string): boolean;
/**
 * Known webhook event types from Wasender.
 */
declare const WasenderWebhookEventType: {
    readonly ChatsUpsert: "chats.upsert";
    readonly ChatsUpdate: "chats.update";
    readonly ChatsDelete: "chats.delete";
    readonly GroupsUpsert: "groups.upsert";
    readonly GroupsUpdate: "groups.update";
    readonly GroupParticipantsUpdate: "group-participants.update";
    readonly ContactsUpsert: "contacts.upsert";
    readonly ContactsUpdate: "contacts.update";
    readonly MessagesUpsert: "messages.upsert";
    readonly MessagesUpdate: "messages.update";
    readonly MessagesDelete: "messages.delete";
    readonly MessagesReaction: "messages.reaction";
    readonly MessageReceiptUpdate: "message-receipt.update";
    readonly MessageSent: "message.sent";
    readonly SessionStatus: "session.status";
    readonly QrCodeUpdated: "qrcode.updated";
};
type WasenderWebhookEventType = typeof WasenderWebhookEventType[keyof typeof WasenderWebhookEventType];
/**
 * Base interface for all Wasender webhook events.
 */
interface BaseWebhookEvent<T extends WasenderWebhookEventType, D = any> {
    /** The type of the event. */
    type: T;
    /** Timestamp of the event generation (example, confirm actual structure). */
    timestamp?: number;
    /** The actual data payload for the event. Can be a single object or an array of objects. */
    data: D | D[];
    /** Session ID or identifier associated with the event (example). */
    sessionId?: string;
}
interface MessageKey {
    id: string;
    fromMe: boolean;
    remoteId: string;
    participant?: string;
}
interface ChatEntry {
    id: string;
    name?: string;
    conversationTimestamp?: number;
    unreadCount?: number;
    muteEndTime?: number;
    isSpam?: boolean;
}
type ChatsUpsertEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.ChatsUpsert, ChatEntry[]>;
type ChatsUpdateEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.ChatsUpdate, Partial<ChatEntry>[]>;
type ChatsDeleteEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.ChatsDelete, string[]>;
interface GroupMetadata {
    id: string;
    subject: string;
    creation?: number;
    owner?: string;
    desc?: string;
    participants?: GroupParticipant[];
    announce?: boolean;
    restrict?: boolean;
}
type GroupsUpsertEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.GroupsUpsert, GroupMetadata[]>;
type GroupsUpdateEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.GroupsUpdate, Partial<GroupMetadata>[]>;

interface GroupParticipantsUpdateData {
    id: string;
    participants: Array<string | GroupParticipant>;
    action: 'add' | 'remove' | 'promote' | 'demote';
}
type GroupParticipantsUpdateEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.GroupParticipantsUpdate, GroupParticipantsUpdateData>;
interface ContactEntry {
    id: string;
    name?: string;
    notify?: string;
    verifiedName?: string;
    status?: string;
    imgUrl?: string;
}
type ContactsUpsertEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.ContactsUpsert, ContactEntry[]>;
type ContactsUpdateEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.ContactsUpdate, Partial<ContactEntry>[]>;
interface MessageContent {
    conversation?: string;
    imageMessage?: {
        url?: string;
        caption?: string;
        mimetype?: string;
    };
    videoMessage?: {
        url?: string;
        caption?: string;
        mimetype?: string;
    };
    documentMessage?: {
        url?: string;
        title?: string;
        mimetype?: string;
        fileName?: string;
    };
    audioMessage?: {
        url?: string;
        mimetype?: string;
        duration?: number;
    };
    stickerMessage?: {
        url?: string;
        mimetype?: string;
    };
    contactMessage?: {
        displayName?: string;
        vcard?: string;
    };
    locationMessage?: {
        degreesLatitude?: number;
        degreesLongitude?: number;
        name?: string;
        address?: string;
    };
}
interface MessagesUpsertData {
    key: MessageKey;
    message?: MessageContent;
    pushName?: string;
    messageTimestamp?: number;
}
type MessagesUpsertEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.MessagesUpsert, MessagesUpsertData>;
interface MessageUpdate {
    status: 'delivered' | 'read' | 'played' | 'error' | 'pending';
}
interface MessagesUpdateDataEntry {
    key: MessageKey;
    update: MessageUpdate;
}
type MessagesUpdateEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.MessagesUpdate, MessagesUpdateDataEntry[]>;
interface MessagesDeleteData {
    keys: MessageKey[];
}
type MessagesDeleteEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.MessagesDelete, MessagesDeleteData>;
interface Reaction {
    text: string;
    key: MessageKey;
    senderTimestampMs?: string;
    read?: boolean;
}
interface MessagesReactionDataEntry {
    key: MessageKey;
    reaction: Reaction;
}
type MessagesReactionEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.MessagesReaction, MessagesReactionDataEntry[]>;
interface Receipt {
    userJid: string;
    status: 'sent' | 'delivered' | 'read' | 'played';
    t?: number;
}
interface MessageReceiptUpdateDataEntry {
    key: MessageKey;
    receipt: Receipt;
}
type MessageReceiptUpdateEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.MessageReceiptUpdate, MessageReceiptUpdateDataEntry[]>;
interface MessageSentData {
    key: MessageKey;
    message?: MessageContent;
    status?: string;
}
type MessageSentEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.MessageSent, MessageSentData>;
interface SessionStatusData {
    status: 'connected' | 'disconnected' | 'connecting' | 'error' | 'logged_out' | 'need_scan';
    session_id?: string;
    reason?: string;
}
type SessionStatusEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.SessionStatus, SessionStatusData>;
interface QrCodeUpdatedData {
    qr: string;
    session_id?: string;
}
type QrCodeUpdatedEvent = BaseWebhookEvent<typeof WasenderWebhookEventType.QrCodeUpdated, QrCodeUpdatedData>;
type WasenderWebhookEvent = ChatsUpsertEvent | ChatsUpdateEvent | ChatsDeleteEvent | GroupsUpsertEvent | GroupsUpdateEvent | GroupParticipantsUpdateEvent | ContactsUpsertEvent | ContactsUpdateEvent | MessagesUpsertEvent | MessagesUpdateEvent | MessagesDeleteEvent | MessagesReactionEvent | MessageReceiptUpdateEvent | MessageSentEvent | SessionStatusEvent | QrCodeUpdatedEvent;

interface Contact {
    /** The id (Jabber ID) of the contact, typically a phone number. */
    id: string;
    /** The name of the contact as saved by the user. */
    name?: string;
    /** The display name of the contact (push name). */
    notify?: string;
    /** The verified business name, if applicable. */
    verifiedName?: string;
    /** URL of the contact\'s profile picture. */
    imgUrl?: string;
    /** The contact\'s status message. */
    status?: string;
    /** Indicates if the contact exists on WhatsApp. */
    exists?: boolean;
}
/** Response for retrieving a list of all contacts. */
interface GetAllContactsResponse extends WasenderSuccessResponse$1 {
    data: Contact[];
}
/** Response for retrieving information about a single contact. */
interface GetContactInfoResponse extends WasenderSuccessResponse$1 {
    data: Contact;
}
/** Response for retrieving a contact\'s profile picture URL. */
interface GetContactProfilePictureResponse extends WasenderSuccessResponse$1 {
    data: {
        imgUrl: string | null;
    };
}
/** Response for blocking or unblocking a contact. */
interface ContactActionResponse extends WasenderSuccessResponse$1 {
    data: {
        message: string;
    };
}
/** Combined result for a successful "get all contacts" API call. */
interface GetAllContactsResult {
    response: GetAllContactsResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
/** Combined result for a successful "get contact info" API call. */
interface GetContactInfoResult {
    response: GetContactInfoResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
/** Combined result for a successful "get contact profile picture" API call. */
interface GetContactProfilePictureResult {
    response: GetContactProfilePictureResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
/** Combined result for a successful contact action (block/unblock) API call. */
interface ContactActionResult {
    response: ContactActionResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}

type WhatsAppSessionStatus = "CONNECTED" | "DISCONNECTED" | "NEED_SCAN" | "CONNECTING" | "LOGGED_OUT" | "EXPIRED";
interface WhatsAppSession {
    id: number;
    name: string;
    phone_number: string;
    status: WhatsAppSessionStatus;
    account_protection: boolean;
    log_messages: boolean;
    webhook_url: string | null;
    webhook_enabled: boolean;
    webhook_events: string[] | null;
    created_at: string;
    updated_at: string;
}
interface CreateWhatsAppSessionPayload {
    name: string;
    phone_number: string;
    account_protection: boolean;
    log_messages: boolean;
    webhook_url?: string | null;
    webhook_enabled?: boolean;
    webhook_events?: string[] | null;
}
interface UpdateWhatsAppSessionPayload {
    name?: string;
    phone_number?: string;
    account_protection?: boolean;
    log_messages?: boolean;
    webhook_url?: string | null;
    webhook_enabled?: boolean;
    webhook_events?: string[] | null;
}
interface ConnectSessionResponseData {
    status: WhatsAppSessionStatus;
    qrCode?: string;
    message?: string;
}
interface QRCodeResponseData {
    qrCode: string;
}
interface DisconnectSessionResponseData {
    status: WhatsAppSessionStatus;
    message: string;
}
interface RegenerateApiKeyResponse extends Omit<WasenderSuccessResponse$1, 'message' | 'data'> {
    success: true;
    api_key: string;
}
interface GetAllWhatsAppSessionsResponse extends WasenderSuccessResponse$1 {
    data: WhatsAppSession[];
}
interface GetWhatsAppSessionDetailsResponse extends WasenderSuccessResponse$1 {
    data: WhatsAppSession;
}
interface CreateWhatsAppSessionResponse extends WasenderSuccessResponse$1 {
    data: WhatsAppSession;
}
interface UpdateWhatsAppSessionResponse extends WasenderSuccessResponse$1 {
    data: WhatsAppSession;
}
interface DeleteWhatsAppSessionResponse extends WasenderSuccessResponse$1 {
    data: null;
}
interface ConnectSessionResponse extends WasenderSuccessResponse$1 {
    data: ConnectSessionResponseData;
}
interface GetQRCodeResponse extends WasenderSuccessResponse$1 {
    data: QRCodeResponseData;
}
interface DisconnectSessionResponse extends WasenderSuccessResponse$1 {
    data: DisconnectSessionResponseData;
}
interface GetSessionStatusResponse {
    status: WhatsAppSessionStatus;
}
interface GetAllWhatsAppSessionsResult {
    response: GetAllWhatsAppSessionsResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface GetWhatsAppSessionDetailsResult {
    response: GetWhatsAppSessionDetailsResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface CreateWhatsAppSessionResult {
    response: CreateWhatsAppSessionResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface UpdateWhatsAppSessionResult {
    response: UpdateWhatsAppSessionResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface DeleteWhatsAppSessionResult {
    response: DeleteWhatsAppSessionResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface ConnectSessionResult {
    response: ConnectSessionResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface GetQRCodeResult {
    response: GetQRCodeResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface DisconnectSessionResult {
    response: DisconnectSessionResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface RegenerateApiKeyResult {
    response: RegenerateApiKeyResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}
interface GetSessionStatusResult {
    response: GetSessionStatusResponse;
    /** Optional. Rate limit information from the API response. May be undefined if not applicable or not provided by the server. */
    rateLimit?: RateLimitInfo;
}

type FetchImplementation = (input: RequestInfo | URL, init?: RequestInit) => Promise<globalThis.Response>;
interface RetryConfig {
    /** Maximum number of retries for rate limit errors (HTTP 429). Defaults to 0 (no retries). */
    maxRetries?: number;
    /** Whether to enable retries on HTTP 429. Defaults to false. */
    enabled?: boolean;
}
/**
 * Interface for abstracting the necessary parts of an incoming HTTP request
 * for webhook processing. Adapt your framework's request object to this.
 */
interface WebhookRequestAdapter {
    /** Function to get a header value by its name (case-insensitive). */
    getHeader: (name: string) => string | undefined | null;
    /** Function that returns the raw request body as a string. */
    getRawBody: () => Promise<string> | string;
}
declare class Wasender {
    private readonly baseUrl;
    private readonly apiKey;
    private readonly personalAccessToken?;
    private readonly fetchImpl;
    private readonly retryConfig;
    private readonly configuredWebhookSecret?;
    constructor(apiKey: string | undefined, personalAccessToken?: string, baseUrl?: string, fetchImplementation?: FetchImplementation, retryOptions?: RetryConfig, webhookSecret?: string);
    private parseRateLimitHeaders;
    private request;
    private postInternal;
    private getInternal;
    private putInternal;
    private deleteInternal;
    /**
     * Sends any valid Wasender message payload.
     * @param payload A WasenderMessagePayload object (e.g., TextOnlyMessage, ImageUrlMessage).
     * @returns A promise that resolves to the API response and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    send<T extends WasenderMessagePayload>(payload: T): Promise<WasenderSendResult>;
    sendText(payload: Omit<TextOnlyMessage, 'messageType'>): Promise<WasenderSendResult>;
    sendImage(payload: Omit<ImageUrlMessage, 'messageType'>): Promise<WasenderSendResult>;
    sendVideo(payload: Omit<VideoUrlMessage, 'messageType'>): Promise<WasenderSendResult>;
    sendDocument(payload: Omit<DocumentUrlMessage, 'messageType'>): Promise<WasenderSendResult>;
    sendAudio(payload: Omit<AudioUrlMessage, 'messageType'>): Promise<WasenderSendResult>;
    sendSticker(payload: Omit<StickerUrlMessage, 'messageType'>): Promise<WasenderSendResult>;
    sendContact(payload: Omit<ContactCardMessage, 'messageType'>): Promise<WasenderSendResult>;
    sendLocation(payload: Omit<LocationPinMessage, 'messageType'>): Promise<WasenderSendResult>;
    /**
     * Retrieves a list of all contacts synced with the WhatsApp session.
     * @returns A promise that resolves to the list of contacts and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    getContacts(): Promise<GetAllContactsResult>;
    /**
     * Retrieves detailed information for a specific contact.
     * @param contactPhoneNumber The JID (Jabber ID) of the contact in E.164 format (e.g., 1234567890).
     * @returns A promise that resolves to the contact information and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    getContactInfo(contactPhoneNumber: string): Promise<GetContactInfoResult>;
    /**
     * Retrieves the URL of the profile picture for a specific contact.
     * @param contactPhoneNumber The JID (Jabber ID) of the contact in E.164 format (e.g., 1234567890).
     * @returns A promise that resolves to the profile picture URL and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    getContactProfilePicture(contactPhoneNumber: string): Promise<GetContactProfilePictureResult>;
    /**
     * Blocks a specific contact.
     * @param contactPhoneNumber The JID (Jabber ID) of the contact in E.164 format (e.g., 1234567890).
     * @returns A promise that resolves to the action status and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    blockContact(contactPhoneNumber: string): Promise<ContactActionResult>;
    /**
     * Unblocks a specific contact.
     * @param contactPhoneNumber The JID (Jabber ID) of the contact in E.164 format (e.g., 1234567890).
     * @returns A promise that resolves to the action status and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    unblockContact(contactPhoneNumber: string): Promise<ContactActionResult>;
    /**
     * Retrieves a list of all WhatsApp groups the connected account is a member of.
     * @returns A promise that resolves to the list of groups and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    getGroups(): Promise<GetAllGroupsResult>;
    /**
     * Retrieves metadata for a specific group.
     * @param groupJid The JID of the group (e.g., '123456789-987654321@g.us').
     * @returns A promise that resolves to the group metadata and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    getGroupMetadata(groupJid: string): Promise<GetGroupMetadataResult>;
    /**
     * Retrieves a list of participants for a specific group.
     * @param groupJid The JID of the group.
     * @returns A promise that resolves to the list of group participants and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    getGroupParticipants(groupJid: string): Promise<GetGroupParticipantsResult>;
    /**
     * Adds participants to a specific group. Requires admin privileges in the group.
     * @param groupJid The JID of the group.
     * @param participants An array of participant JIDs (E.164 format phone numbers) to add.
     * @returns A promise that resolves to the action status for each participant and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    addGroupParticipants(groupJid: string, participants: string[]): Promise<ModifyGroupParticipantsResult>;
    /**
     * Removes participants from a specific group. Requires admin privileges in the group.
     * @param groupJid The JID of the group.
     * @param participants An array of participant JIDs (E.164 format phone numbers) to remove.
     * @returns A promise that resolves to the action status for each participant and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    removeGroupParticipants(groupJid: string, participants: string[]): Promise<ModifyGroupParticipantsResult>;
    /**
     * Updates settings for a specific group.
     * @param groupJid The JID of the group.
     * @param settings An object containing the settings to update (e.g., subject, description, announce, restrict).
     * @returns A promise that resolves to the updated group settings information and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    updateGroupSettings(groupJid: string, settings: UpdateGroupSettingsPayload): Promise<UpdateGroupSettingsResult>;
    /**
     * Retrieves a list of all WhatsApp sessions available to the authenticated user.
     * @returns A promise that resolves to the list of WhatsApp sessions and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    getAllWhatsAppSessions(): Promise<GetAllWhatsAppSessionsResult>;
    /**
     * Creates a new WhatsApp session.
     * @param payload Details for the new session.
     * @returns A promise that resolves to the created session information and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    createWhatsAppSession(payload: CreateWhatsAppSessionPayload): Promise<CreateWhatsAppSessionResult>;
    /**
     * Retrieves details for a specific WhatsApp session.
     * @param sessionId ID of the WhatsApp session.
     * @returns A promise that resolves to the session details and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    getWhatsAppSessionDetails(sessionId: number): Promise<GetWhatsAppSessionDetailsResult>;
    /**
     * Updates details for a specific WhatsApp session.
     * @param sessionId ID of the WhatsApp session.
     * @param payload Fields to update.
     * @returns A promise that resolves to the updated session details and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    updateWhatsAppSession(sessionId: number, payload: UpdateWhatsAppSessionPayload): Promise<UpdateWhatsAppSessionResult>;
    /**
     * Deletes a specific WhatsApp session.
     * @param sessionId ID of the WhatsApp session.
     * @returns A promise that resolves to the deletion confirmation and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    deleteWhatsAppSession(sessionId: number): Promise<DeleteWhatsAppSessionResult>;
    /**
     * Initiates the connection process for a WhatsApp session.
     * @param sessionId ID of the WhatsApp session.
     * @param qrAsImage Optional. If true, requests the QR code as an image in the response.
     * @returns A promise that resolves to the connection status (e.g., QR code) and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    connectWhatsAppSession(sessionId: number, qrAsImage?: boolean): Promise<ConnectSessionResult>;
    /**
     * Retrieves the QR code for connecting a WhatsApp session.
     * @param sessionId ID of the WhatsApp session.
     * @returns A promise that resolves to the QR code data and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    getWhatsAppSessionQRCode(sessionId: number): Promise<GetQRCodeResult>;
    /**
     * Disconnects an active WhatsApp session.
     * @param sessionId ID of the WhatsApp session.
     * @returns A promise that resolves to the disconnection status and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    disconnectWhatsAppSession(sessionId: number): Promise<DisconnectSessionResult>;
    /**
     * Regenerates the API key for a specific WhatsApp session.
     * Note: The response structure for this endpoint is unique.
     * @param sessionId ID of the WhatsApp session.
     * @returns A promise that resolves to the new API key and rate limit information.
     * @throws WasenderAPIError if the request fails.
     */
    regenerateApiKey(sessionId: number): Promise<RegenerateApiKeyResult>;
    /**
     * Retrieves the current status of the WhatsApp session associated with the API key.
     * Note: The response structure for this endpoint is unique and does not include a session ID.
     * @returns A promise that resolves to the session status. Rate limiting may or may not apply as per API docs.
     * @throws WasenderAPIError if the request fails.
     */
    getSessionStatus(): Promise<GetSessionStatusResult>;
    /**
     * Verifies and parses an incoming Wasender webhook event.
     * @param request An object that adapts your HTTP framework's request, providing methods to get headers and the raw body.
     * @returns A promise that resolves to the parsed WasenderWebhookEvent.
     * @throws WasenderAPIError if the webhook secret is not configured in the SDK, if the signature is invalid, or if the body cannot be parsed.
     */
    handleWebhookEvent(request: WebhookRequestAdapter): Promise<WasenderWebhookEvent>;
}
declare const createWasender: (apiKey: string | undefined, personalAccessToken?: string, baseUrl?: string, fetchImplementation?: FetchImplementation, retryOptions?: RetryConfig, webhookSecret?: string) => Wasender;

interface WasenderErrorDetail {
    [key: string]: string[];
}
interface WasenderErrorResponse {
    success: false;
    message: string;
    errors?: WasenderErrorDetail;
    retry_after?: number;
}
interface WasenderSuccessResponse {
    success: true;
    message: string;
}
type WasenderAPIRawResponse = WasenderSuccessResponse | WasenderErrorResponse;
/**
 * Custom error class for errors originating from the Wasender API or the SDK itself (e.g., network issues).
 */
declare class WasenderAPIError extends Error {
    /** HTTP status code from the API response, if available. Undefined for network errors or other non-HTTP issues. */
    readonly statusCode?: number;
    /** Detailed field-specific errors from the API response, if provided (e.g., validation errors). */
    readonly errorDetails?: WasenderErrorDetail;
    /** Suggested number of seconds to wait before retrying, from API response body (e.g., for rate limiting). */
    readonly retryAfter?: number;
    readonly success: false;
    /** The 'message' field from the API error response body, or a descriptive message for SDK-originated errors. */
    readonly apiMessage: string;
    /** Rate limit information parsed from response headers, if available at the time of error. */
    readonly rateLimit?: RateLimitInfo;
    constructor(apiMessage: string, statusCode?: number, errorDetails?: WasenderErrorDetail, retryAfter?: number, rateLimit?: RateLimitInfo);
}

export { type AudioMessage, type AudioUrlMessage, type BaseMessage, type BaseWebhookEvent, type ChatEntry, type ChatsDeleteEvent, type ChatsUpdateEvent, type ChatsUpsertEvent, type ContactCard, type ContactCardMessage, type ContactCardPayload, type ContactEntry, type ContactMessage, type ContactsUpdateEvent, type ContactsUpsertEvent, type DocumentMessage, type DocumentUrlMessage, type FetchImplementation, type GroupMetadata, type GroupParticipantsUpdateData, type GroupParticipantsUpdateEvent, type GroupsUpdateEvent, type GroupsUpsertEvent, type ImageMessage, type ImageUrlMessage, type LocationMessage, type LocationPin, type LocationPinMessage, type LocationPinPayload, type MessageContent, type MessageKey, type MessageReceiptUpdateDataEntry, type MessageReceiptUpdateEvent, type MessageSentData, type MessageSentEvent, type MessageUpdate, type MessagesDeleteData, type MessagesDeleteEvent, type MessagesReactionDataEntry, type MessagesReactionEvent, type MessagesUpdateDataEntry, type MessagesUpdateEvent, type MessagesUpsertData, type MessagesUpsertEvent, type QrCodeUpdatedData, type QrCodeUpdatedEvent, type RateLimitInfo, type Reaction, type Receipt, type RetryConfig, type SessionStatusData, type SessionStatusEvent, type StickerMessage, type StickerUrlMessage, type TextMessage, type TextOnlyMessage, type VideoMessage, type VideoUrlMessage, WEBHOOK_SIGNATURE_HEADER, Wasender, WasenderAPIError, type WasenderAPIRawResponse, type WasenderErrorDetail, type WasenderErrorResponse, type WasenderMessagePayload, type WasenderSendResult, type WasenderSuccessResponse$1 as WasenderSuccessResponse, type WasenderWebhookEvent, WasenderWebhookEventType, type WebhookRequestAdapter, createWasender, verifyWasenderWebhookSignature };
